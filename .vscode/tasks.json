{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Push Full Project to GitHub",
      "type": "shell",
      "command": "& { $date = Get-Date -Format 'yyyy-MM-dd HH:mm:ss'; git pull; if ($?) { git add .; git commit -m ('Full project update ' + $date); git push } }",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      },
      "options": {
        "shell": {
          "executable": "powershell.exe",
          "args": ["-NoProfile", "-ExecutionPolicy", "Bypass", "-Command"]
        },
        "statusbar": {
          "hide": false
        }
      },
      "runOptions": {
        "runOn": "default"
      },
      "detail": "Syncs with remote, then pushes full project to GitHub"
    }
  ]
}